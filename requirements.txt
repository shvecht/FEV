# --- Core runtime (viewer + EDF I/O) ---
numpy>=1.24,<3
PySide6>=6.6,<7
pyqtgraph>=0.13,<0.14
pyEDFlib>=0.1.36,<0.2
zarr>=2.16,<3

# --- Nice-to-haves (optional; uncomment when needed) ---
scipy>=1.10,<2           # higher-quality decimation, filtering (Phase 6)
pandas>=2.0,<3           # if you want CSV/TSV annotations via pandas
pyarrow>=14,<18          # parquet for annotations later (Phase 9+)
zarr>=2.16,<3            # multi-resolution cache store (Phase 9+)
numcodecs>=0.12,<0.13    # compression for Zarr chunks
msgspec>=0.18,<0.19      # fast JSON/structs if you add config manifests

# --- Dev tooling (keep commented if you want bare minimum) ---
black>=24.1,<25
isort>=5.13,<6
mypy>=1.8,<2
pytest>=8,<9
